Build a config screen for this Plasma6 widget.
First row: a dropdown list of virtual desktops. When collapsed, show desktop name. When expanded, show desktop name and
a preview of the widget.

second row: a preview of the widget of the selected desktop with clickable elements:
- background date and background number: open color selector
- day name text: open dialog with font and color selectors
- day date text: open dialog with font and color selectors
- desktop number text: open dialog with font and color selectors

How do I open the config screen?

Does not work.

Dropdownbox is not working.

Dropdownbox shows only Arbeitsfläche 6

Problems:
- controls are disabeld on first open
- only Arbeitsfläche 6 is shown
- the desktop numer in the dropdown preview is wrong
- the dropdown preview is not updated when settings are changed

Problems:
- only Arbeitsfläche 6 is shown
- the dropdown preview is gone

The preview in the dropdown shall not open any dialogs.

The widget does not use the settings.

main.qml:27:3: color is not a type

Remove hover help.

In the font dialog, don't show the size.

In the font dialog, add a preview of the font.

In the font dialog, use the text of the target element as preview text.

In the font dialog, center the preview text.

In the font dialog, display preview above the font selector.

If a new virtual desktop is created, this happens: main.qml:243: Error: Cannot assign [undefined] to QString

If a new virtual desktop is created, widget, dropdown and preview do not match.

If a new virtual desktop is created, widget, dropdown and preview do not match.

main.qml:54:5: Cannot assign to non-existent default property

The widget does not work anymore. Settings for new virtual desktops are not saved.

The widget does not use the settings.

main.qml:46:3: Connections is not a type

main.qml:101: ReferenceError: desktopInfo is not defined

Add a new section to the widget: name of desktop.

Desktop name is not shown (also in the dropdown and preview).

Desktop name is not shown anywhere.

Changing fonts and colors in the preview does not work.

Changing fonts and colors in the preview does not work.

Clicking on the preview does nothing.

Add Fonts and colors for desktop name section to settings.

Add background color for desktop name section to settings.